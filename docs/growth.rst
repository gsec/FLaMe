growth module
=============

Progressions, Ideas
~~~~~~~~~~~~~~~~~~~
Id 1:
  Merge the idea of grid as acess points and special lists for everything.
  Create a grid structure with small integers at the index `i`, `j` and `k`.
  Those integers each refer to special lists as class attributes (e.g.
  `surface`, `atoms`, etc.)

.. automodule:: growth
    :members:
    :undoc-members:
    :show-inheritance:
